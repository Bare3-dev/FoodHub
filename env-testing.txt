# Testing Environment Configuration
# Add these to your .env.testing file or set in phpunit.xml

# Broadcasting Driver for Testing
BROADCAST_CONNECTION=log

# Pusher Configuration (for WebSocket broadcasting in production)
PUSHER_APP_ID=test_app_id
PUSHER_APP_KEY=test_app_key
PUSHER_APP_SECRET=test_app_secret
PUSHER_APP_CLUSTER=mt1

# Database for Testing
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Cache for Testing
CACHE_DRIVER=array
SESSION_DRIVER=array
QUEUE_CONNECTION=sync
